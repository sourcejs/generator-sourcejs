{
  "name": "sourcejs-<%= name %>",
  "version": "0.1.0",
  <% if (author) { %>"author": "<%= author %>",<% } %>
  "description": "<%= description %>",
  "dependencies": {
    "deep-extend": "^0.3.2"
  }
}